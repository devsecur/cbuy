import { CSSProperties } from 'react';

export interface IconPropsType {
    color?: string 
    stroke?: string;
    width?: number;
    height?: number;
  
    // eslint-disable-next-line
    props?: any;
  }
  